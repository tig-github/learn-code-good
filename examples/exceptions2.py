# However, this will be a NameError - when a variable is not defined but is used. This happens during
# runtime, so it is only detected after the print statement occureed


print("Hello World!")
print(string_variable) # my editor has highlighting that warns of these errors
 

